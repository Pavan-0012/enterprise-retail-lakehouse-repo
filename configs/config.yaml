paths:
  raw: data/raw
  bronze: data/bronze
  silver: data/silver
  gold: data/gold

storage:
  provider: minio

minio:
  endpoint: localhost:9000
  access_key: admin
  secret_key: password123
  secure: false

bucket:
  name: enterprise-retail-lakehouse